<h1>It's Time for a Node-Eat-Da-Burger!</h1>
<h2>Place your burger name order here!</h2>

<ul>
  {{#each burger}}
    {{#if name}}
      {{> name/devoured = true}}
    {{/if}}
  {{/each}}
</ul>

<ul>
  {{#each burger}}
    {{#unless name}}
      {{> name/not-devoured = true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Add Your Burger Name</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger:</label>
    <input type="text" id="burger" name="name">
  </div>
d
  <div class="form-group">
    <label for="name">Name?</label><br>
    <input type="text" name="name"><br>
  </div>

  <div class="dev" id="devoured">
  </div>

  <div class="dev" id="not-devoured">
  </div>

  <button type="submit">Devour It!</button>
</form>
